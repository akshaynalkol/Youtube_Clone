import { createSlice } from "@reduxjs/toolkit";

const searchVideoSlice = createSlice({
    name: 'searchVideo',
    initialState: {
        video: [],
    },
    reducers: {
        addVideos: (state) => {
            
        }
    }
})